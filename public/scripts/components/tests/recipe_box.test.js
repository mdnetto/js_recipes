describe('recipe box', () => {

	const recipe_box = require('../recipe_box');

	test('', () => {
	});

	//test('loads recipes from server', () => {
	//		expect(recipe_box).toBe("<RecipeBox .. >");
	//});

});

